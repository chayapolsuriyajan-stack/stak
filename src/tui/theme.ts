/**
 * ANSI "gray" renders too dark to read on many terminal themes (especially
 * light backgrounds and low-contrast dark ones). A fixed lighter hex reads
 * consistently everywhere Ink's `color` prop is used for muted/secondary text.
 */
export const MUTED = "#9ca3af";
export const ACCENT = "#a5b4fc";
